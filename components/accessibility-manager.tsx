"use client";
import { useEffect } from "react";
const focusable='button:not([disabled]),a[href],input:not([disabled]),select:not([disabled]),textarea:not([disabled]),[tabindex]:not([tabindex="-1"])';
export function AccessibilityManager(){useEffect(()=>{let previous:HTMLElement|null=null;const observer=new MutationObserver(()=>{const dialog=document.querySelector<HTMLElement>('[role="dialog"][aria-modal="true"]');if(dialog&&!dialog.dataset.focusManaged){dialog.dataset.focusManaged="true";previous=document.activeElement as HTMLElement;window.setTimeout(()=>dialog.querySelector<HTMLElement>(focusable)?.focus(),0)}});observer.observe(document.body,{childList:true,subtree:true});const keydown=(event:KeyboardEvent)=>{const dialog=document.querySelector<HTMLElement>('[role="dialog"][aria-modal="true"]');if(!dialog)return;if(event.key==="Escape"){const close=dialog.querySelector<HTMLButtonElement>('button[aria-label^="Close"]');if(close){event.preventDefault();close.click();previous?.focus();return}}if(event.key!=="Tab")return;const items=[...dialog.querySelectorAll<HTMLElement>(focusable)].filter(item=>item.offsetParent!==null);if(!items.length)return;const first=items[0],last=items.at(-1)!;if(event.shiftKey&&document.activeElement===first){event.preventDefault();last.focus()}else if(!event.shiftKey&&document.activeElement===last){event.preventDefault();first.focus()}};document.addEventListener("keydown",keydown);return()=>{observer.disconnect();document.removeEventListener("keydown",keydown)}},[]);return null}
